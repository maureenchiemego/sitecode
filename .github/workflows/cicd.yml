name: CI/CD Pipeline

on:
  push:
    branches:
      - main

jobs:
  deploy-to-server:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Display code
        run: ls -la

      - name: Display more things
        run: |
          ls -la
          hostnamectl

